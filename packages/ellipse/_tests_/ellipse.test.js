const { mount } = require('@vue/test-utils')
describe('Xz-Ellipse', ()=>{
    describe('Xz-VectorLayer', ()=>{
        test('adds 1 + 2 to equal 3', () => {
            expect(1+2).toBe(3);
        })
    })
})